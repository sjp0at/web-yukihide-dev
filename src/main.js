import './style.css';

// Small animation logic for cards if needed in the future
document.querySelectorAll('.card').forEach(card => {
  card.addEventListener('mouseenter', () => {
    // We could add more dynamic effects here
  });
});
